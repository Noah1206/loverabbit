/*
  돈을 받기 전에 모델을 부르지 않는다.

  무료 미리보기는 8/24~8/26 사흘 동안 리딩 115건을 만들어 $10.21 을 썼고, 그중
  팔린 것은 4건이었다. 같은 기간 매출은 17,870원, AI 값은 18,257원이다 — 만드는
  쪽이 파는 쪽보다 컸다. 리딩 한 건의 원가는 중앙 107원이라 한 건씩 보면 작지만,
  안 사는 사람이 사는 사람의 스무 배라 합계가 뒤집힌다.

  그래서 관문을 앞으로 옮긴다. 폼을 다 채우면 로그인하고, 1,900원을 결제하고,
  입금이 확인되는 순간 그때 만든다. 안 낸 사람에게는 모델을 한 번도 부르지 않는다.

  명식·지수·목차는 그대로 계산해서 보여 준다. 그건 공짜다 — 계산이지 생성이
  아니다. 돈을 요구하기 전에 "이 도구가 내 사주를 실제로 세웠다"는 것만은
  보여 줘야 한다.

  글은 /api/unlock 이 만든다. 그 자리는 원래도 "없으면 만들어서 준다" 였고
  (reading-finish.ts), 재개 정보의 doneSections 가 0 이면 처음부터 만든다.
  발급이 재개 정보를 남기지 않으면 그 길이 끊긴다 — 반드시 함께 저장해야 한다.

  되돌리려면 이 값을 false 로 둔다. 그러면 예전처럼 익명으로 미리보기를 만든다.
*/
export const PAY_BEFORE_GENERATE = true;
