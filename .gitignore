node_modules/
.next/
data/
out/
.env
.env.local
*.tsbuildinfo
next-env.d.ts

.vercel
.env*
!.env.example
.agents/
.claude/
skills-lock.json
public/cards-pastel-original/

# 모델 비교 결과 (scripts/model-compare.mts) — 리딩 원문이 들어 있다
.model-compare.json

# 리딩 미리보기 (scripts/reading-preview.mts) — 유료 본문이 통째로 들어 있다
.reading-preview.*.json
public/generated/
data/reading-images/

# Threads 승인 대기열 (scripts/threads-drafts.mts) — 미승인 초안이 들어 있다.
# 승인 전 원고가 저장소에 섞이면 어느 것이 나갔는지 구분이 안 된다.
.threads-drafts.json

# 예약 발행 러너 로그
.threads-publish.log

# 워커 실행 로그
worker/worker.log

# 힉스필드 카드 모션 원본 — 편당 수 MB. 웹에 나가는 것은 압축본
# public/cards-motion/ 쪽이고, 그건 저장소에 넣는다.
marketing/video/cards-raw/

# 표정 클립 원본 — 웹에 나가는 것은 public/characters/motion*/ 쪽 압축본
marketing/video/emotions-raw/
marketing/video/emotions-raw-adult/

# 성인 등급 프롬프트 — 내용은 저장소에 넣지 않는다 (틀은 example 파일 참조)
marketing/video/adult-prompts.json

# 동작 줄(사람이 채우는 원본) — 내용은 저장소에 넣지 않는다
marketing/video/actions.json

# 동작 줄(사람이 채우는 원본)
marketing/video/actions.txt

# 메타 업로드용으로 광고별로 모아 둔 소재 — 전부 다른 폴더에 있는 것의 복사본이다.
# 같은 그림을 저장소에 두 번 넣을 이유가 없다. collect-meta-upload.mjs 로 다시 만든다.
marketing/ads/meta-upload-v1/

# src/data 는 코드가 import 하는 기준 데이터다. 위의 data/ 규칙(로컬 저장소)에
# 걸려 통째로 빠지면 배포 빌드가 깨진다.
!src/data/
!src/data/**
